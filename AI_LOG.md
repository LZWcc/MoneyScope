# AI_LOG.md

本文件用于记录 MoneyScope 项目中 AI 辅助开发的过程。请在每次使用 AI 完成环境配置、功能实现、调试、文档整理或测试分析后及时补充记录。

## 使用的 AI 工具

- ChatGPT / Codex / Claude Code
- 其他工具：暂无

## 记录模板

````md
### 记录编号

- 日期：
- 使用工具：
- 使用场景：
- 原始提示词：

```text
在这里粘贴关键提示词。
```

- AI 输出摘要：
- 人工修改说明：
- 修改原因：
- 反思与收获：
````

## 记录 001：项目初始化建议与目录搭建

- 日期：2026-05-24
- 使用工具：Codex
- 使用场景：根据 Python 课程项目要求，确定 MoneyScope 初始化方案并创建项目基础结构。
- 原始提示词：

```text
这是我的 python 小组作业。请根据课程项目要求 md，给出初始化建议。
开始吧，你不要帮我 commit。
```

- AI 输出摘要：
  - 建议将项目定位为 Python 数据应用项目。
  - 推荐使用 Python + Streamlit + SQLite + pandas + plotly。
  - 说明 Streamlit 更符合 Python 课程评分重点，Vue 全栈方案会增加工作量并削弱 Python 主线。
  - 初始化 README、CLAUDE、AI_LOG、docs 和 app 等基础目录文件。
- 人工修改说明：
  - 后续开发时需要由小组成员补充真实仓库地址、小组分工、运行截图和测试结果。
- 修改原因：
  - 初始文档只提供项目骨架和开发规范，避免过早写死所有实现细节。
- 反思与收获：
  - Python 课程项目应优先保证 Python 技术应用、数据持久化、面向对象设计和文档完整性，再考虑前端视觉效果。

## 记录 002：成员 A 核心开发文档规划

- 日期：2026-05-24
- 使用工具：Codex
- 使用场景：作为成员 A，在正式实现代码前梳理 MoneyScope MVP 的 PRD、设计规格和实施计划。
- 原始提示词：

```text
现在我是成员 A 核心开发 负责主要代码实现、项目结构维护、功能整合和最终检查
请开始开发, 写好plan, specs, prd等
```

- AI 输出摘要：
  - 阅读项目根目录 `CLAUDE.md`、README、需求分析、数据库设计和当前代码占位状态。
  - 判断当前仓库适合先完成“文档先行”的 MVP 规划。
  - 编写本地 PRD、MVP 设计规格和后续实现计划，明确数据库、模型、服务、图表、Streamlit UI、测试和文档更新范围。
- 人工修改说明：
  - PRD 暂时保存为仓库本地文档，不发布到外部 issue tracker。
  - 后续实现时仍需成员 A 根据课堂要求和实际运行结果调整报告、PPT 和测试记录。
- 修改原因：
  - 当前项目没有配置 issue tracker，课程项目更需要可直接提交和解释的本地文档。
  - 先明确范围可以避免一次性堆功能导致代码难以维护或答辩难以说明。
- 反思与收获：
  - 课程项目应先保证稳定 MVP 和清晰分层，再考虑自动分类、备份恢复等可选功能。
  - 成员 A 的核心职责不仅是写代码，也包括维护结构、整合功能和确保文档与实际实现一致。

## 记录 003：数据库层与模型层实现

- 日期：2026-05-24
- 使用工具：Claude Code
- 使用场景：根据数据库设计文档，实现 SQLite 连接、三张表初始化、默认分类写入，以及 Transaction 和 Budget 数据类。
- 原始提示词：

```text
请根据 CLAUDE.md 的数据库设计，实现 app/database.py 和 app/models.py，
包括建表、默认分类、Transaction 和 Budget 数据类，字段校验放在 __post_init__，
所有错误信息用中文。
```

- AI 输出摘要：
  - 实现了 `get_connection`（自动建目录、row_factory）和 `initialize_database`（幂等建表、INSERT OR IGNORE 写默认分类）。
  - 使用 Python `dataclass` 定义 `Transaction` 和 `Budget`，在 `__post_init__` 中调用 `utils.py` 工具函数完成校验。
  - 工具函数统一放 `app/utils.py`，包括日期、月份、金额、类型、分类校验，错误信息全部用中文。
- 人工修改说明：
  - 将 `DATABASE_PATH` 提取到单独的 `app/config.py`，避免多处硬编码路径。
  - 调整 `budgets` 表的 UNIQUE 约束，覆盖 `(month, category)` 联合键。
- 修改原因：
  - 配置集中管理，方便后续修改数据库路径或部署到其他环境。
  - 总预算（category 为 NULL）在 SQLite 中需要手动处理唯一性，AI 初稿遗漏了这一边界情况。
- 反思与收获：
  - SQLite 的 NULL 不参与 UNIQUE 约束是常见的坑，需要在服务层额外判断再做 upsert。
  - dataclass + **post_init** 是 Python 课程项目实现"有意义的类"的清晰模式。

## 记录 004：服务层业务逻辑实现

- 日期：2026-05-24
- 使用工具：Claude Code
- 使用场景：实现交易 CRUD、月度统计、分类汇总、每日趋势、预算保存与检查、CSV 导入导出。
- 原始提示词：

```text
请实现 app/services.py，包括：
- add/list/delete_transaction
- get_monthly_summary / get_category_expense_summary / get_daily_trend
- save_budget / check_budget_warnings
- import_transactions_csv / export_transactions_csv
所有查询返回 pandas DataFrame，错误信息用中文。
```

- AI 输出摘要：
  - `list_transactions` 支持按月份、类型、分类动态组合筛选，使用 `pd.read_sql_query` 直接返回 DataFrame。
  - `get_daily_trend` 生成覆盖整月的完整日期序列，缺交易的日期补 0，保证趋势图横轴稳定。
  - `check_budget_warnings` 对总预算和分类预算分别判断，超 100% 提示"已超过"，超 80% 提示"接近"。
  - `import_transactions_csv` 逐行校验，失败行记录行号和中文原因，不中断其余行。
  - `export_transactions_csv` 使用 `utf-8-sig` 编码保证中文在 Excel 正常显示。
- 人工修改说明：
  - `save_budget` 对总预算（category=None）增加了手动查询+更新的逻辑，避免 SQLite NULL 唯一约束失效。
  - 月份筛选使用 `substr(date, 1, 7) = ?` 而不是 LIKE，更严格。
- 修改原因：
  - AI 初稿对 NULL 的 upsert 判断有误，直接用 INSERT OR REPLACE 会导致 id 变化。
- 反思与收获：
  - pandas + SQLite 组合做统计很方便，但 NULL 处理和编码问题需要人工校对。

## 记录 005：Streamlit 界面搭建

- 日期：2026-05-24
- 使用工具：Claude Code
- 使用场景：实现 app/main.py，包括六个标签页和侧边栏月份选择。
- 原始提示词：

```text
请实现 app/main.py 的 Streamlit 界面，六个标签页：概览、添加记录、
交易明细、统计分析、预算设置、导入导出。UI 文案全部中文，
错误用 st.error 显示，不要把 SQL 写进 main.py。
```

- AI 输出摘要：
  - 使用 `st.tabs` 实现六标签页，侧边栏 `text_input` 控制全局月份。
  - 「添加记录」页面的类型与分类联动控件放在 form 外部，切换类型时触发重跑。
  - 表单提交后使用 `st.toast` + `st.rerun()` 刷新页面，避免数据不同步。
  - 交易明细表格使用 `st.column_config` 设置中文列名和数字格式。
- 人工修改说明：
  - 添加"其他（自定义）"分类选项，让用户可以输入系统分类列表之外的分类。
  - 删除控件改用 `st.columns([3, 1], vertical_alignment="bottom")` 对齐输入框与按钮。
  - 结余卡片使用 `delta` 参数着色，正数显示绿色，负数显示红色。
- 修改原因：
  - 预设分类列表在实际使用中不够灵活，允许自定义输入体验更好。
  - AI 初稿的删除区域按钮与输入框高度不对齐，视觉上杂乱。
- 反思与收获：
  - Streamlit 的 form 内控件不会触发即时重跑，联动控件必须放到 form 外。
  - `st.rerun()` 能保证保存后立即刷新概览，但要注意避免无限循环。

## 记录 006：图表层实现

- 日期：2026-05-24
- 使用工具：Claude Code
- 使用场景：实现 app/charts.py 的分类饼图和每日收支趋势图。
- 原始提示词：

```text
请实现 app/charts.py，包括分类支出饼图（px.pie）和月度每日收支趋势图
（go.Scatter 三条线），横轴固定到当月 1 号至月末，空数据返回空图不崩溃。
```

- AI 输出摘要：
  - 饼图使用 `plotly.express.pie`，空数据时返回带标题的空 `go.Figure`。
  - 趋势图使用 `go.Scatter` 添加收入、支出、结余三条折线，`mode="lines+markers"`。
  - 横轴使用 `pd.offsets.MonthEnd(0)` 计算月末，`tickformat="%m-%d"` 只显示月日。
- 人工修改说明：
  - 初稿趋势图横轴改为类别轴（category），导致刻度按秒钟格式显示，修改为 `type="date"` 并手动设置 range 解决。
- 修改原因：
  - plotly 自动推断轴类型时，对稀疏日期数据会选错轴类型，手动锁定类型更稳定。
- 反思与收获：
  - plotly 图表的横轴类型需要明确指定，不能依赖自动推断，尤其是日期数据稀疏的情况。

## 记录 007：pytest 测试覆盖

- 日期：2026-05-24
- 使用工具：Claude Code
- 使用场景：编写 tests/test_services.py，覆盖模型校验、数据库初始化、服务层 CRUD、统计、预算和 CSV 全流程。
- 原始提示词：

```text
请为 MoneyScope 编写 pytest 测试，覆盖：
- Transaction / Budget 校验
- 数据库初始化
- 交易新增/筛选/删除
- 月度统计、分类统计、每日趋势
- 预算超额提醒
- CSV 导入导出
使用 tmp_path 隔离数据库，不要污染真实 data/moneyscope.db。
```

- AI 输出摘要：
  - 21 个测试用例，按功能分 6 组，全部使用 `tmp_path` 临时数据库。
  - 每日趋势测试断言整月覆盖（五月 31 天，二月 28 天），无交易日期补 0。
  - CSV 导入测试用 `io.StringIO` 模拟文件对象，不依赖真实文件。
- 人工修改说明：
  - 无重大修改，修正了个别断言中的列名拼写。
- 修改原因：
  - AI 初稿基本可用，主要是对齐服务层的实际返回字段名。
- 反思与收获：
  - 使用 `tmp_path` 是 pytest 隔离数据库测试的标准方案，简洁有效。
  - 21 个测试全部通过，对后续重构提供了基础回归保障。

## 记录 008：Streamlit 仪表盘界面优化

- 日期：2026-05-25
- 修改人：idnx
- 使用工具：Codex
- 使用场景：在不破坏原有功能的前提下，优化 MoneyScope 的首页、侧边栏、指标卡片、图表区和交易明细显示。
- AI 输出摘要：
  - 新建并切换到 `idnx-ui-improve` 分支。
  - 阅读 `app/main.py`、`app/charts.py`、`app/services.py`，确认服务层仍负责业务逻辑。
  - 优化 `app/main.py` 的全局 CSS、侧边栏导航、页面标题、首页仪表盘、指标卡片、图表布局和交易明细表格高度。
  - 优化 `app/charts.py` 的 Plotly 主题，使饼图和趋势图更适合仪表盘展示。
- 人工修改说明：
  - 未新增依赖，未修改数据库层、模型层和服务层。
  - 对插入 HTML 的展示文本做转义，避免用户自定义分类影响页面结构。
- 修改原因：
  - 原界面功能完整，但首页信息层级较简单，课程展示时仪表盘感不足。
  - 保持 Streamlit 简洁实现，同时通过 CSS 和布局提升视觉质量。
- 反思与收获：
  - Streamlit UI 优化应集中在展示层，统计数据仍通过服务层函数获取。
  - 小步修改、及时测试可以降低界面调整对原有功能的影响。

## 记录 009：首页空状态展示优化

- 日期：2026-05-26
- 修改人：idnx
- 使用工具：Codex
- 使用场景：优化 MoneyScope 首页在没有交易数据时的展示效果，减少重复的暂无数据横幅。
- AI 输出摘要：
  - 在 `app/main.py` 新增 `render_empty_state` 通用空状态卡片，支持 icon、title、description 和可选按钮文案。
  - 首页没有交易数据时增加欢迎引导卡片，引导用户添加第一笔记录或导入 CSV。
  - 将预算提醒、图表分析和最近交易的空状态从大横幅改为小型卡片。
  - 调整全局 CSS 的 section 间距、卡片圆角、阴影、边框和空状态字体大小。
- 人工修改说明：
  - 未修改服务层和数据库层，所有统计数据仍通过 `services.py` 获取。
  - 空状态按钮文案作为引导展示，不改变导航与表单提交逻辑。
- 修改原因：
  - 原首页在空数据时多个区域重复显示暂无数据，视觉上偏粗糙。
  - 小型空状态卡片能让空数据页面更像完整的数据分析系统。
- 反思与收获：
  - 空状态也是产品体验的一部分，应提供下一步引导，而不只是提示没有数据。
  - 对课程项目而言，保持功能稳定的同时优化展示细节，更利于演示和答辩。

## 记录 010：隐藏 Streamlit 英文菜单并固定浅色主题

- 日期：2026-05-26
- 修改人：idnx
- 使用工具：Codex
- 使用场景：隐藏 Streamlit 自带英文工具栏菜单，并让本地预览固定使用浅色主题。
- AI 输出摘要：
  - 新增 `.streamlit/config.toml`，设置 `client.toolbarMode = "minimal"` 并指定浅色主题颜色。
  - 在 `app/main.py` 全局 CSS 中隐藏 Streamlit 自带的 `Deploy`、右上角菜单、状态控件和页脚。
  - 保留 MoneyScope 自己的中文导航、表单、图表和数据表功能。
- 人工修改说明：
  - 未改动服务层、数据库层和业务逻辑。
  - 主题配置需要重启 Streamlit 后生效。
- 修改原因：
  - Streamlit 自带菜单默认是英文，容易影响中文课程项目展示。
  - 固定浅色主题可以避免浏览器或系统深色模式导致表格和表单对比异常。
- 反思与收获：
  - 项目级 `.streamlit/config.toml` 适合管理主题和工具栏模式。
  - 对 Streamlit 自带 UI 的隐藏应只针对框架菜单，不影响应用主体功能。

## 记录 011：优化交易记录删除入口

- 日期：2026-05-26
- 修改人：idnx
- 使用工具：Codex
- 使用场景：用户希望在点错录入数据时，可以更方便地删除某一条交易记录。
- AI 输出摘要：
  - 检查现有 `delete_transaction` 服务函数和交易明细页删除区域。
  - 在 `app/main.py` 的交易明细页增加“从当前筛选结果中选择要删除的记录”下拉框。
  - 保留原来的手动输入编号删除方式作为备用。
  - 删除成功后继续使用 `st.toast` 和 `st.rerun()` 刷新页面。
- 人工修改说明：
  - 未改动 `services.py` 的删除逻辑，仍然调用已有 `delete_transaction()`。
  - 下拉选项显示编号、日期、类型、分类、金额和备注，便于确认要删除的记录。
- 修改原因：
  - 只输入编号删除不够直观，用户需要先在表格里找到 id。
  - 从当前筛选结果选择记录更符合“点错后删除这一条”的使用场景。
- 反思与收获：
  - 删除功能需要降低误操作概率，展示足够多的信息帮助用户确认。
  - 在已有服务函数上增强 UI，比新增业务逻辑更稳妥。

## 记录 012：删除后重排交易编号

- 日期：2026-05-26
- 修改人：idnx
- 使用工具：Codex
- 使用场景：用户反馈删除交易后编号出现跳号，希望编号回退并保持连续。
- AI 输出摘要：
  - 修改 `app/services.py` 的 `delete_transaction()`，删除成功后重排 `transactions` 表 id。
  - 同步更新 SQLite 的 `sqlite_sequence`，保证下一次新增记录继续使用连续编号。
  - 新增测试覆盖删除中间记录后编号连续，以及下一次新增编号正确。
- 人工修改说明：
  - 保留页面层删除入口不变，编号连续逻辑集中在服务层。
  - 重排时先把 id 临时改为负数，再改回正序，避免主键冲突。
- 修改原因：
  - 对课程项目和用户界面来说，“编号”更像可读序号，跳号会让用户误以为数据异常。
  - 将编号重排放在服务层，可以让手动删除和下拉删除行为保持一致。
- 反思与收获：
  - 真实生产系统通常不建议修改主键，但本项目没有交易外键引用，重排编号更符合当前用户体验。
  - 涉及数据库编号规则时必须增加回归测试，避免后续新增记录再次跳号。

## 记录 013：优化删除记录区域界面

- 日期：2026-05-26
- 修改人：idnx
- 使用工具：Codex
- 使用场景：用户反馈交易明细页删除记录区域视觉粗糙，希望优化界面。
- AI 输出摘要：
  - 将删除区从“大输入框 + 大按钮”改为“选择记录 + 待删除记录确认卡片 + 删除按钮”。
  - 保留手动输入编号删除，但移动到折叠面板中，减少默认页面占用空间。
  - 为待删除记录展示编号、日期、类型、分类、金额、备注和创建时间，降低误删概率。
- 人工修改说明：
  - 未修改删除业务逻辑，仍调用 `delete_transaction()`。
  - 删除后编号连续的提示保留在删除区中，增强用户预期。
- 修改原因：
  - 原删除区域视觉重心过强，且手动输入编号默认展开不够友好。
  - 先选择再确认记录信息，更符合危险操作的交互习惯。
- 反思与收获：
  - 删除类操作不仅要可用，还要让用户清楚知道自己将删除哪条数据。
  - 把低频备用能力折叠起来，可以让主流程更干净。

## 记录 014：修复侧边栏收起后无法展开的 Bug

- 日期：2026-06-01
- 使用工具：Claude Code
- 使用场景：用户反馈侧边栏收起后再也无法展开，需要排查 CSS 隐藏规则对侧边栏按钮的影响。
- 原始提示词：

```text
现在侧边栏收进去就再也出不来了，请修改这个bug
```

- AI 输出摘要：
  - 初步分析：`header { height: 0 !important; }` 将顶部栏高度压为零，隐藏了侧边栏展开按钮。移除该规则后问题仍存在。
  - 深入排查：通过查阅 Streamlit 1.57.0 前端源码，确认侧边栏折叠按钮 (`stSidebarCollapseButton`) 在侧边栏内部，而展开按钮 (`stExpandSidebarButton`) 渲染在主工具栏 `stToolbar` 内部。
  - 根因：CSS 中 `[data-testid="stToolbar"]` 和 `[data-testid="baseButton-headerNoPadding"]` 两条规则把整个工具栏及其子按钮隐藏，展开按钮跟着消失。
  - 最终方案：将 `stToolbar` 改为 `height: 0; overflow: visible` 折叠工具栏但保留子元素可溢出，再用 `position: fixed` 将展开按钮固定在左上角，确保始终可见可点击。
- 人工修改说明：
  - 无额外人工修改。
- 修改原因：
  - 原 CSS 为了隐藏 Streamlit 英文菜单，误伤了侧边栏展开按钮所在的工具栏。
  - 需要精准隐藏不需要的 UI 元素，同时保留侧边栏交互功能。
- 反思与收获：
  - Streamlit 的侧边栏展开按钮不在侧边栏内部，而是在主工具栏中，不能用 `[data-testid="stSidebar"]` 相关选择器保护它。
  - 用 `display: none` 隐藏父元素时，子元素的 `display` 无法覆盖，必须改用 `height: 0 + overflow: visible` 或 `position: fixed` 让子元素脱离父级布局。
  - 对框架自带 UI 做 CSS 隐藏时，应先通过浏览器开发者工具或源码确认目标元素的完整 DOM 结构，避免误伤。

## 记录 015：生成演示数据库

- 日期：2026-06-01
- 使用工具：Claude Code
- 使用场景：需要在 data 目录下创建多个演示数据库，以便在前端界面切换查看不同数据场景的展示效果。
- 原始提示词：

```text
现在帮我写几个演示数据库放在data下，以能够在前端界面上看到不同数据下的显示
```

- AI 输出摘要：
  - 编写 `data/create_demo_dbs.py` 脚本，生成三个演示数据库：
    - `demo_full.db`：3 个月 28 条交易 + 预算设置，展示完整仪表盘、图表趋势和分类分布。
    - `demo_budget_warning.db`：当月 9 条交易，5 个分类全部超预算，展示满屏红色告警。
    - `demo_minimal.db`：仅 3 条记录，展示空状态卡片和简洁界面。
  - 使用 `cp data/demo_xxx.db data/moneyscope.db` 切换演示场景。
- 人工修改说明：
  - 无额外人工修改。
- 修改原因：
  - 单一数据库无法展示课程演示需要的多种场景（完整数据、预算告警、空状态）。
  - 独立的演示数据库可以在不修改代码的情况下切换展示效果。
- 反思与收获：
  - 演示数据库应覆盖功能的边界场景，而不只是正常数据，这样答辩时能更全面地展示系统能力。
  - 用脚本生成数据库比手动操作 SQLite 更可重复、可维护。

## 记录 016：将界面 emoji 替换为 SVG/Unicode 图标

- 日期：2026-06-01
- 使用工具：Claude Code
- 使用场景：用户反馈界面中的 emoji 图标廉价，希望换成更美观的图标。
- 原始提示词：

```text
请帮我把一些廉价的emoji换成美观的icon图标
```

- AI 输出摘要：
  - 在 `app/main.py` 常量区新增 `_SVG` 字典，收录 11 个 stroke 风格 SVG 图标（箭头、对勾、饼图、折线、加号、垃圾桶、红色/黄色警告）。
  - 导航菜单 `NAV_ITEMS` 的 emoji 替换为精致 Unicode 符号（▣ ✎ ≡ ∿ ◎ ⇅）。
  - `metric_card` 的 ↑ ↓ = 替换为 SVG 上箭头、下箭头、等号，`metric-icon` 的 `font-size` 改为 0 避免字体渲染干扰。
  - `render_empty_state` 的所有 emoji 替换为对应 SVG，`empty-state-icon` 同步调整 CSS。
  - 预算提醒的 🔴🟡 替换为红色圆形警告 SVG 和黄色三角警告 SVG，内嵌在 `st.warning` 文字中。
  - `page_icon` 从 💰 改为 `$`，`st.info` 的 📭 前缀直接删除。
  - 按钮和 toast 中的 💾 🗑️ ✅ 🚀 ⬇️ ⚠️ 保留，属于原生组件文字，视觉上不突兀。
- 人工修改说明：
  - 无额外人工修改。
- 修改原因：
  - emoji 在不同操作系统和浏览器下渲染差异大，stroke SVG 风格统一、清晰，与整体设计语言更一致。
- 反思与收获：
  - 自定义 HTML 区域可以完全控制图标，但 Streamlit 原生组件（`st.radio` 标签、`st.button` 文字）只支持纯文本，需要区分处理。
  - SVG 图标需要在父容器设置 `font-size: 0` 消除行高影响，并确保 `currentColor` 能正确继承父元素颜色。

## 记录 017：将侧边栏导航从 st.radio 改为 st.button

- 日期：2026-06-01
- 使用工具：Claude Code
- 使用场景：侧边栏导航每个选项的点击横向区域过小，多次调整 radio CSS 无效，改用 st.button 实现导航。
- 原始提示词：

```text
现在左侧导航每一个选项的点击横向区域过小，请调整
```

- AI 输出摘要：
  - 第一轮：尝试在 `[data-testid="stRadio"]` 的各层 div 上补 `width: 100%`，无效。
  - 第二轮：加 `display: flex; flex-direction: column; align-items: stretch` 和 `> div > div { width: 100% }`，仍无效。
  - 根因分析：Streamlit radio 的 DOM 结构不透明，CSS 无法可靠控制每个选项的宽度，继续猜结构效率极低。
  - 最终方案：将导航从 `st.radio` 改为 `st.button` 循环，每个按钮设 `use_container_width=True`，天然全宽。
  - 用 `st.session_state.page` 记录当前页，选中的按钮用 `type="primary"`，CSS 覆盖为浅蓝灰高亮，未选中保持透明背景。
  - 全局 button 样式限定在 `[data-testid="stSidebar"]` 内，不影响主内容区的保存/删除等按钮。
- 人工修改说明：
  - 无额外人工修改。
- 修改原因：
  - Streamlit 的 radio 组件内部 DOM 层级多且不稳定，CSS 覆盖需要猜测结构，维护成本高。
  - st.button 的宽度行为完全可控，是更可靠的导航实现方式。
- 反思与收获：
  - 对 Streamlit 原生组件做深度 CSS 定制时，应先通过浏览器 DevTools 确认真实 DOM 结构，而不是反复猜测。
  - 当 CSS 方案两次尝试仍无效时，应果断换实现方案，而不是继续堆 CSS。
  - st.button + session_state 是 Streamlit 中实现自定义导航的标准模式，比 radio 更灵活。

## 记录 018：修复 UI 空白问题与功能增强

- 日期：2026-06-01
- 使用工具：Claude Code
- 使用场景：修复概览页和统计分析页图表区域空白、预算页缺少列表和删除功能、添加记录页功能单薄等多个前端问题。
- 原始提示词：

```text
检查我现在代码中存在的问题，概览页图表分析板块当没有支出时有空白，
预算设置页不能清晰看到设置了哪些预算分类、不能删除预算，以及寻找其他问题。
```

- AI 输出摘要：
  - 概览页和统计分析页：将图表区域从固定双列布局改为自适应布局，只有两侧都有数据时才分列，否则全宽展示，消除空白。
  - 移除 CSS 中 `.empty-state-card` 无效的 `height: 100%` 属性。
  - 预算页：新增 `list_budgets()` 和 `delete_budget()` 服务函数，页面下方展示已有预算表格和删除操作。
  - 预算页右侧提醒区域：区分"尚未设置预算"和"支出状况良好"两种空状态。
  - 添加记录页：新增快捷记账按钮（使用 `st.popover` 弹出迷你表单）、最近 5 条记录展示、分类管理区域（添加/删除分类）。
  - 在 `database.py` 新增 `add_category()` 和 `delete_category()` 函数。
- 人工修改说明：
  - 快捷记账最初尝试通过 session_state 预填 selectbox，因 Streamlit widget 状态管理限制失败，改为 popover 方案。
  - 分类删除最初放在 form 外导致视觉不对齐，后改为 form 内统一样式。
- 修改原因：
  - 图表区域空白是因为 Streamlit 列布局不会自动对齐高度，一侧空状态卡片远矮于另一侧图表。
  - 预算页缺少基本的查看和删除功能，用户无法管理已设置的预算。
  - 添加记录页过于简单，增加快捷入口和分类管理提升实用性。
- 反思与收获：
  - Streamlit 的 selectbox 使用 key 参数时，外部设置 session_state 值不一定能覆盖 widget 内部状态，popover 是更可靠的快捷输入方案。
  - form 内的 widget 值只在提交时更新，动态联动（如切换类型刷新分类列表）在 form 内无法实现，需要权衡视觉一致性和交互灵活性。
  - 自适应布局比固定双列更健壮，能优雅处理数据缺失的边界情况。

## 记录 019：拆分 main.py 为小模块

- 日期：2026-06-01
- 使用工具：Claude Code
- 使用场景：main.py 已有 1386 行，包含 CSS、常量、组件函数和 6 个页面，代码可读性差，需要拆分为独立模块。
- 原始提示词：

```text
注意到现在main.py有1000多行，能否拆分为一个个小模块？
```

- AI 输出摘要：
  - 分析 main.py 结构，识别出常量、CSS、组件函数、页面渲染函数和入口逻辑五个自然边界。
  - 拆分为 10 个文件：`constants.py`（类型映射、SVG 图标）、`styles.py`（CSS）、`components.py`（通用 UI 组件）、`_pages/` 下 6 个页面文件、精简后的 `main.py`（仅入口和路由）。
  - 修复测试中对 `app.main.resolve_budget_category` 的引用，改为从 `app.components` 导入。
  - 修复 Streamlit 自动扫描 `pages/` 目录导致侧边栏显示文件名的问题，将目录重命名为 `_pages/`。
- 人工修改说明：
  - 无额外人工修改。
- 修改原因：
  - 单文件 1386 行难以阅读和维护，拆分后每个文件不超过 170 行，职责清晰。
  - Streamlit 约定 `pages/` 目录为多页面入口，使用 `_pages/` 避免自动检测。
- 反思与收获：
  - Streamlit 会自动将 `pages/` 目录下的 `.py` 文件注册为导航页面，自定义导航时应避开这个目录名。
  - 拆分大文件时，先用 Agent 并行创建新模块再精简原文件，比逐行搬移效率更高。
  - 拆分后立即运行测试能快速发现导入路径变更导致的断裂。
